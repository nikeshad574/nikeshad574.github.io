import type { Key } from "react";

export interface IExperience {
  id: Key;
  description: string;
  time: string;
  orderByYear: string;
  title: string;
  created_at: string;
  updated_at: string;
}
